CREATE TABLE users
    (
        user_id INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL,
        username VARCHAR(20) NOT NULL,
        password VARCHER(500) NOT NULL,
        user_fname VARCHAR(100) NOT NULL,
        user_lname VARCHAR(100) NOT NULL,
        email VARCHAR(100) NOT NULL
    );
