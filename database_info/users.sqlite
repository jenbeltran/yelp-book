CREATE TABLE users
    (
        user_id INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL,
        user_fname VARCHAR(100),
        user_lname VARCHAR(100),
        email VARCHAR(100)
    );

INSERT INTO users(user_fname, user_lname, email)
VALUES
('Jen', 'Beltran', 'jen@yorku.ca');