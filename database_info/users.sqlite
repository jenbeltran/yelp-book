CREATE TABLE users
    (
        user_id INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL,
        username VARCHAR(20),
        password VARCHAR(20),
        user_fname VARCHAR(100),
        user_lname VARCHAR(100),
        email VARCHAR(100)
    );

INSERT INTO users(username, password, user_fname, user_lname, email)
VALUES
('j3nx2', 'password', 'Jen', 'Beltran', 'jen@yorku.ca');